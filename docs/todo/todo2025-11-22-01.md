# TODO 2025-11-22 01

## 主題：BuildingAI 專案初始理解與掃描計畫

### [問題標題]

- BuildingAI 專案上手：建立初始理解與後續掃描／文件產出計畫

### [背景與重現步驟]

- 目前狀態：
    - 新接手 `d:/app/BuildingAI` 專案，尚未有完整的 `PROJECT_OVERVIEW.md` 與 `FRONTEND_ARCH.md`。
    - 已閱讀並理解以下規則與範本檔：
        - `docs/rule/1-1ELF_EXPRESS_RULES.md`
        - `docs/rule/1-2ELF_AI_ONBOARDING.md`
        - `docs/rule/1-3ELF_API_OVERVIEW.md`
        - `docs/rule/1-4todoYYYY-MM-DD-XX.md`
- 目標：
    - 先規劃安全的專案掃描路線與後續文件產出流程，**本 todo 的當前階段不修改任何程式碼**。

### [相關檔案路徑與定位]

- 規則與流程說明檔：
    - `docs/rule/1-1ELF_EXPRESS_RULES.md`（專案通用協作規則）
    - `docs/rule/1-2ELF_AI_ONBOARDING.md`（AI 上手流程與文件結構規則）
    - `docs/rule/1-3ELF_API_OVERVIEW.md`（API 總覽與模組對照規則）
    - `docs/rule/1-4todoYYYY-MM-DD-XX.md`（每日 todo 模板與開發流程圖）
- 預計在後續掃描階段閱讀的專案檔案（此處僅列為計畫，尚未實際閱讀）：
    - 專案說明與入口：
        - `README.md`
    - 建置與套件資訊：
        - `package.json`
        - （若存在）`pnpm-lock.yaml` / `yarn.lock` / `package-lock.json`
    - 建置與開發設定：
        - `vite.config.*` 或其他 bundler 設定檔（如 `webpack.config.*` 等）
        - `tsconfig.*`
    - 前端啟動與架構相關：
        - 入口檔：例如 `src/main.ts` 或 `src/main.js`
        - 路由設定：例如 `src/router/**`
        - 狀態管理：例如 `src/store/**`、`src/pinia/**`

### [分析與方案]

- 分析：
    - 目前對 BuildingAI 專案的 domain 與業務範圍尚未有完整理解，需要先透過 README、目錄結構與建置設定取得第一版全貌。
    - 依
      `ELF_AI_ONBOARDING.md`，本 todo 應先完成「分析與方案」，具體程式碼修改與文件產出可以在後續階段（同一檔的後半段或新的 todo）進行。

- 方案 A（**採用**）：小步掃描＋文件驅動的專案理解流程
    1. **Step 1：掃描專案根目錄結構**
        - 由 AI 協助列出 `d:/app/BuildingAI` 主要資料夾與關鍵檔案。
        - 標記與前端、後端、設定、文件相關的目錄（例如 `src/`、`docs/`、`config/` 等）。

    2. **Step 2：閱讀核心說明與建置檔**
        - 依序閱讀：
            - `README.md`：專案簡介、啟動方式、主要功能描述（若有）。
            - `package.json`：scripts、dependencies、devDependencies，推敲前端框架與工具鏈（例如 Vue/Vite 等）。
            - 主要建置與型別設定檔：`vite.config.*`、`tsconfig.*` 及其他存在的 config。
        - 預計在後續的 `docs/PROJECT_OVERVIEW.md` 中整理：
            - 專案目標與 domain（若能從文件或程式判斷）。
            - 前後端技術棧與主要開發／build 指令。
            - 目錄結構大綱與各資料夾職責簡介。

    3. **Step 3：閱讀前端啟動與路由架構**
        - 入口檔：`src/main.ts` 或 `src/main.js`，確認：
            - App 啟動流程（建立 app、掛載 router、store、全域元件等）。
        - 路由：`src/router/**`，了解：
            - 路由分區（如 system、onlineDev、extend 等）。
            - 是否有動態載入或權限導向的路由設計。
        - 狀態管理：`src/store/**` 或 `src/pinia/**`，了解：
            - 使用哪種狀態管理方式（Vuex / Pinia / 自行封裝等）。
        - 預計在後續的 `docs/FRONTEND_ARCH.md` 中整理：
            - App 啟動流程（條列或簡易 flow）。
            - Router 結構與主要模組。
            - 狀態管理策略與關鍵模組位置。

    4. **Step 4：視需要規劃 feature 級的 `ui-flow-<feature>.md`**
        - 暫不立即新增任何
          `ui-flow-*.md`，只在未來遇到需要深入理解的複雜核心功能（例如大型 refactor、設計器、關鍵授權流程等）時，再以新主題 todo 提出並新增對應文件。

    5. **Step 5：全程以 todo 檔紀錄理解過程**
        - 本檔 `todo2025-11-22-01` 目前專注在「整體初始理解與掃描計畫」。
        - 後續實際掃描與閱讀時，視主題複雜度選擇：
            - 繼續在本檔補充理解與結果，或
            - 針對特定功能／bug 開立新的 `todoYYYY-MM-DD-XX.md`，依相同格式紀錄。

- 方案 B（**不採用**）：直接從單一 bug／功能切入專案
    - 作法：不先建立整體理解，直接從某個需求或 bug 著手，邊改邊理解架構。
    - 風險：
        - 容易在缺乏全局觀點下做出局部最佳化甚至危險改動。
        - 不符合 `ELF_AI_ONBOARDING.md` 中建議的「先 PROJECT_OVERVIEW /
          FRONTEND_ARCH、後 feature 級 ui-flow」的順序。
    - 結論：暫不採用，僅保留作為未來針對特定問題時新開 todo 的策略之一。

- 結論：
    - 採用 **方案 A**
      的小步掃描與文件驅動理解流程，確保後續任何實作或 refactor 都建立在清晰的專案觀之上，並符合
      `ELF_EXPRESS_RULES.md` 的「最小改動、安全、可回滾」原則。

### [實作重點摘要]

- 本階段仍未修改任何程式碼或設定檔內容，只進行專案結構與關鍵檔案的閱讀與整理。
- 已完成的工作：
    - 掃描 `d:/app/BuildingAI` 根目錄結構，確認為使用 pnpm + Turbo 的 monorepo，主要目錄包含：
        - `packages/`（核心程式碼與子套件）
        - `extensions/`（擴充功能模組）
        - `docs/`（文件與規則）
        - `public/`、`scripts/`、`docker/` 等與執行／部署相關的目錄。
    - 閱讀 `README.md`，取得專案 domain 與特性：
        - 企業級開源智能 Agent 平台，面向 AI 開發者、AI 創業者與企業，用於打造原生 AI 應用（包含 Agent、MCP、RAG、知識庫、會員／計費等能力）。
        - 推薦透過 Docker Compose 部署，啟動後預設從 `http://localhost:4090/install` 進入安裝精靈。
    - 閱讀根目錄 `package.json`，確認：
        - 專案名稱為 `buildingai-monorepo`，使用 pnpm + Turbo 管理多套件。
        - 主要指令：
            - `pnpm dev`（對應 `turbo run dev`）
            - `pnpm build`（對應 `turbo run build`）
            - `pnpm docker:up` / `pnpm docker:down` 等部署相關指令。
        - 前端採用 Nuxt（`nuxt` 由 `catalog:web` 提供）、Vue 3、Pinia、Nuxt
          UI、TailwindCSS 等技術棧。
    - 閱讀 `pnpm-workspace.yaml`，確認：
        - workspace packages 主要分布於：
            - `packages/*`
            - `packages/web/*`
            - `packages/@buildingai/*`
            - `packages/web/@buildingai/*`
        - 透過 `catalog` / `catalogs`
          統一管理 api/web/dev 等套件版本，方便在 monorepo 中維持一致性。
    - 閱讀 `turbo.json`，確認：
        - 全域任務如 `build`、`lint`、`check-types`、`clean`、`dev` 的依賴與快取策略。
        - `dev` 任務設為 `cache: false` 且 `persistent: true`，表示開發伺服器會常駐且不快取結果。
    - 閱讀前端相關設定檔：
        - `packages/web/buildingai-ui/nuxt.config.ts`
        - `packages/web/@buildingai/nuxt/src/nuxt.config.ts`（透過 `defineBuildingAIConfig`
          提供共用 Nuxt 設定）
        - 初步結論：
            - 前端主應用是位於 `packages/web/buildingai-ui` 的 Nuxt 專案，使用 `@buildingai/nuxt`
              封裝共用設定與模組（Pinia、Nuxt UI、i18n 等）。
            - `buildingai-ui` 專案將 `./app` 目錄別名為 `@` / `~`，開發伺服器預設埠為 4091。
    - 初步查看 Pinia store 實作：
        - `packages/web/@buildingai/stores/src/app.ts`：管理站點設定、登入設定與系統資訊，並提供系統初始化檢查等邏輯。
        - `packages/web/@buildingai/stores/src/user.ts`：管理使用者登入 token、暫存 token、使用者資訊與登入／登出導向邏輯。
        - 確認前端狀態管理採用 Pinia，並與 `@buildingai/service/**` API 互動取得資料。
    - 新增前端／總覽文件初版：
        - `docs/PROJECT_OVERVIEW.md`：整理專案目標與 Domain、技術棧概要、根目錄結構、啟動與 build 流程，以及環境變數重點。
        - `docs/FRONTEND_ARCH.md`：整理 Nuxt 前端應用的啟動流程（`nuxt.config.ts`、`app/app.vue`）、全域路由 middleware（`app/middleware/route.global.ts`）、console 佈局（`app/layouts/console.vue`）與共用 Pinia
          stores 職責，並預留後續補充 pages 路由分區與 extensions 結構的 TODO 小節。
    - 針對 Step 3「前端啟動與路由架構」補充細節（已集中記錄於 `docs/FRONTEND_ARCH.md`）：
        - `app/app.vue`：說明如何在應用啟動時載入站點設定與登入設定、設定 Head /
          SEO、注入主題 CSS 變數，並透過 `apiRecordAnalyse` 紀錄 PAGE_VISIT 行為。
        - `app/middleware/route.global.ts`：整理系統初始化檢查（/install 流程）、登入與導向控制（login
          / home / 403）、以及如何根據後端 menus 動態註冊 console 路由與做權限檢查。
        - `app/layouts/console.vue`：記錄 console 佈局如何依螢幕寬度在 Sidebar / Mixture
          layout 間切換，並使用 cookie 維持使用者偏好的佈局模式。

### [驗證與結果]

- 目前**仍未執行**任何 `npm run dev`、`npm run build`
  或測試指令，本 todo 的工作範圍僅到「閱讀與結構掃描＋文件初版」，不涉及 build 或執行環境驗證。
- 本階段驗證結果偏向「資訊整理」層級：
    - 已確認專案為基於 **NestJS + TypeORM + PostgreSQL** 的後端，與 **Nuxt 4 + Vue 3 + Pinia + Nuxt
      UI + TailwindCSS** 的前端 monorepo。
    - 已確認前端主要設定與啟動相關檔案集中於：
        - `packages/web/buildingai-ui/nuxt.config.ts`
        - `packages/web/@buildingai/nuxt/src/nuxt.config.ts`
    - 已確認全域 store（app / user 等）位於 `packages/web/@buildingai/stores/src`，並已在
      `docs/FRONTEND_ARCH.md` 中整理其職責概要。
    - 已初步掃描 `packages/web/buildingai-ui/app/pages/`
      的路由分區（index/403/[...all]/install/login/console/chat/profile/public/agreement/micropage/webview），並在
      `docs/FRONTEND_ARCH.md` 的 5.1 小節完成高階整理。
- 下一步 TODO（依本檔方案 A，留待未來新 todo 視需求展開）：
    - 若需要更細緻的前端理解：
        - 針對 `app/pages/console/**` 各子模組逐一閱讀，將路由與實際功能對應關係補充進
          `docs/FRONTEND_ARCH.md` 或針對單一模組開 `ui-flow-<feature>.md`。
    - 若需要更細緻的後端／業務理解：
        - 閱讀 `packages/api/**`、`packages/core/**`
          中與目標需求最相關的模組，並在新開的 todo 檔中記錄分析與方案，再視情況補充
          `docs/API_OVERVIEW.md` 或其他後端文件。
