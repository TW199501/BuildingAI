# TODO 2025-11-22 05

## 主題：Feature C：chat 對話 flow（前端流程）

### [問題標題]

- 梳理 chat 對話相關頁面與元件的 UI flow 與狀態／資料流，將結果整理到 `docs/ui-flow-chat.md`：
  - 對話列表／訊息列表如何呈現。
  - 使用者輸入、發送訊息、接收模型回應的整體流程。
  - 相關 store / composable / service（例如 chat 專用 store 或 API 呼叫）。

### [背景與重現步驟]

- 目前狀態：
  - A（install）與 B（login/auth）的前端＋後端流程，已分別在：
    - `docs/ui-flow-install.md`
    - `docs/ui-flow-login.md`
    - 以及 `todo2025-11-22-02/03/04.md` 中完成第一輪記錄。
  - `docs/FRONTEND_ARCH.md` 已對 `app/pages/chat/` 有高階路由區塊標記，但尚未深入 chat 這塊的實際 UI 與資料流細節。
- 目標：
  - 針對 chat 專區（`app/pages/chat/**`）進行第一次前端層級的深讀，先不看後端 API 實作細節，只梳理：
    - 畫面流（使用者如何進入 chat、切換聊天、查看歷史訊息）。
    - 狀態流（store 或 composable 如何管理目前對話、訊息、loading、錯誤等）。
    - 對後端的呼叫節點（API 名稱與用途），API 欄位細節可留待未來 C-API 深入專用 todo。

### [相關檔案路徑與定位]

- 前端 chat 頁面與相關檔案（預計）：
  - `packages/web/buildingai-ui/app/pages/chat/**`
  - `packages/web/buildingai-ui/app/stores/chat.ts` 或其它 chat 相關 store（若存在）。
  - `packages/web/@buildingai/service/webapi/**` 中與 chat / 對話相關的 API（例如對話 send/stream 等）。

### [分析與方案]

- 分析：
  - chat 對話流程是使用者最核心的體驗之一，通常會涉及：
    - 對話列表（多個會話）。
    - 訊息列表（單一會話內訊息流）。
    - 模型選擇、工具使用、狀態提示（loading / streaming / error）。
  - 為避免一次展開太廣，本 todo 僅專注在：
    - 前端畫面與狀態流。
    - 呼叫到哪些 API（僅記名稱與用途，不展開後端實作）。
  - 之後若需要針對 chat 的後端（C-API）進一步分析，可在未來另開 `todoYYYY-MM-DD-XX`。

- 方案：分三步閱讀與整理

  1. **Step 1：掃描與閱讀 `app/pages/chat/**`**

     - 目標：
       - 找出 chat 入口頁（例如 `index.vue` 或其它主頁）。
       - 看清楚有哪些子路由／子元件（對話列表、訊息視圖、設定面板等）。
     - 作法：
       - 先用檔案樹或搜尋列出 `app/pages/chat/` 底下的檔案。
       - 針對主頁與核心子元件做第一輪閱讀，記錄：
         - 使用到的 layout / `definePageMeta`。
         - 主要組件結構與 slot。

  2. **Step 2：整理 chat 狀態管理與 API 呼叫點**

     - 目標：
       - 找出 chat 流程中使用的 store / composable。
       - 梳理對後端的呼叫點（send message、load history 等）。
     - 作法：
       - 從 chat 頁面中找 `useXXX` composable 或 `useXXXStore`。
       - 進一步打開相關 store / composable 檔案，記錄：
         - 內部 state（目前會話 ID、訊息陣列、loading 狀態、錯誤狀態等）。
         - 對 `@buildingai/service/webapi/**` 的呼叫（API 名稱與用途）。

  3. **Step 3：回填 `docs/ui-flow-chat.md`**

     - 目標：
       - 產出第一版 chat 前端 UI flow 文件。
     - 作法：
       - 依照 A/B 的文件風格，至少包含：
         - 流程目的與範圍。
         - 路由與畫面流。
         - 狀態與資料流（包含與後端 API 的介面）。
         - 重要畫面與元件清單。
       - 僅記錄目前實際閱讀到的內容，若有未讀區塊用 TODO 標示避免過度猜測。

### [實作重點摘要]

- 本檔目前僅在「分析與方案」階段，尚未新增或修改任何程式碼，也尚未實際閱讀 chat 頁面與相關 store/composable。
- 已完成的工作：
  - 明確定義本 todo 的範圍僅限 **Feature C（chat）相關的前端 UI 與狀態／資料流**（不含後端 API 實作）。
  - 規劃三步閱讀路線：
    - 先掃描 `app/pages/chat/**` 的頁面與元件結構。
    - 再整理 chat 狀態管理與 API 呼叫點。
    - 最後回填 `docs/ui-flow-chat.md`，以 A/B 同樣結構產出第一版文件。

### [驗證與結果]

- 目前尚未執行任何 `pnpm dev`、`pnpm build` 或測試指令，本 todo 僅針對「chat 前端 UI flow 深入分析」做規劃。
- 後續閱讀完成後，預期會在本節補上：
  - 主要 chat 畫面與路由關係描述。
  - chat 狀態管理（store/composable）的重點欄位與事件。
  - 與後端 chat API 的呼叫節點摘要。
