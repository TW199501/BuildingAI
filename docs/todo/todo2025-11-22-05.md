# TODO 2025-11-22 05

## 主題：Feature C：chat 對話 flow（前端流程）

### [問題標題]

- 梳理 chat 對話相關頁面與元件的 UI flow 與狀態／資料流，將結果整理到 `docs/ui-flow-chat.md`：
  - 對話列表／訊息列表如何呈現。
  - 使用者輸入、發送訊息、接收模型回應的整體流程。
  - 相關 store / composable / service（例如 chat 專用 store 或 API 呼叫）。

### [背景與重現步驟]

- 目前狀態：
  - A（install）與 B（login/auth）的前端＋後端流程，已分別在：
    - `docs/ui-flow-install.md`
    - `docs/ui-flow-login.md`
    - 以及 `todo2025-11-22-02/03/04.md` 中完成第一輪記錄。
  - `docs/FRONTEND_ARCH.md` 已對 `app/pages/chat/` 有高階路由區塊標記，但尚未深入 chat 這塊的實際 UI 與資料流細節。
- 目標：
  - 針對 chat 專區（`app/pages/chat/**`）進行第一次前端層級的深讀，先不看後端 API 實作細節，只梳理：
    - 畫面流（使用者如何進入 chat、切換聊天、查看歷史訊息）。
    - 狀態流（store 或 composable 如何管理目前對話、訊息、loading、錯誤等）。
    - 對後端的呼叫節點（API 名稱與用途），API 欄位細節可留待未來 C-API 深入專用 todo。

### [相關檔案路徑與定位]

- 前端 chat 頁面與相關檔案（預計）：
  - `packages/web/buildingai-ui/app/pages/chat/**`
  - `packages/web/buildingai-ui/app/stores/chat.ts` 或其它 chat 相關 store（若存在）。
  - `packages/web/@buildingai/service/webapi/**` 中與 chat / 對話相關的 API（例如對話 send/stream 等）。

### [分析與方案]

- 分析：
  - chat 對話流程是使用者最核心的體驗之一，通常會涉及：
    - 對話列表（多個會話）。
    - 訊息列表（單一會話內訊息流）。
    - 模型選擇、工具使用、狀態提示（loading / streaming / error）。
  - 為避免一次展開太廣，本 todo 僅專注在：
    - 前端畫面與狀態流。
    - 呼叫到哪些 API（僅記名稱與用途，不展開後端實作）。
  - 之後若需要針對 chat 的後端（C-API）進一步分析，可在未來另開 `todoYYYY-MM-DD-XX`。

- 方案：分三步閱讀與整理

  1. **Step 1：掃描與閱讀 `app/pages/chat/**`**

     - 目標：
       - 找出 chat 入口頁（例如 `index.vue` 或其它主頁）。
       - 看清楚有哪些子路由／子元件（對話列表、訊息視圖、設定面板等）。
     - 作法：
       - 先用檔案樹或搜尋列出 `app/pages/chat/` 底下的檔案。
       - 針對主頁與核心子元件做第一輪閱讀，記錄：
         - 使用到的 layout / `definePageMeta`。
         - 主要組件結構與 slot。

  2. **Step 2：整理 chat 狀態管理與 API 呼叫點**

     - 目標：
       - 找出 chat 流程中使用的 store / composable。
       - 梳理對後端的呼叫點（send message、load history 等）。
     - 作法：
       - 從 chat 頁面中找 `useXXX` composable 或 `useXXXStore`。
       - 進一步打開相關 store / composable 檔案，記錄：
         - 內部 state（目前會話 ID、訊息陣列、loading 狀態、錯誤狀態等）。
         - 對 `@buildingai/service/webapi/**` 的呼叫（API 名稱與用途）。

  3. **Step 3：回填 `docs/ui-flow-chat.md`**

     - 目標：
       - 產出第一版 chat 前端 UI flow 文件。
     - 作法：
       - 依照 A/B 的文件風格，至少包含：
         - 流程目的與範圍。
         - 路由與畫面流。
         - 狀態與資料流（包含與後端 API 的介面）。
         - 重要畫面與元件清單.
       - 僅記錄目前實際閱讀到的內容，若有未讀區塊用 TODO 標示避免過度猜測.

### [實作重點摘要]

- 仍未修改任何程式碼，本 todo 僅進行閱讀與文件更新.
- 已完成的工作：
  - 明確定義本 todo 的範圍僅限 **Feature C（chat）相關的前端 UI 與狀態／資料流**（不含後端 API 實作）。
  - 依原先規劃的三步路線完成第一輪實作：
    - **Step 1：掃描與閱讀 `app/pages/chat/**` 與核心元件**
      - 閱讀 `app/pages/chat/[id].vue`：
        - 確認 `/chat/:id` 作為單一會話主畫面入口，使用 `useAsyncData` 讀取歷史訊息（`apiGetAiConversation`）、會話細節（`apiGetAiConversationDetail`）、chat 設定（`apiGetChatConfig`）。
        - 使用 `useChat({ api: apiChatStream, initialMessages, body: { modelId, mcpServers, conversationId, ... } })` 管理 `messages` / `input` / `files` / `status` / `error` 與提交流程.
      - 閱讀 `components/ask-assistant-chat/chats-chats.vue`：
        - 確認會話列表透過 `apiGetAiConversationList` 載入，並依 `updatedAt` 分組顯示，支援重新命名與刪除會話.
      - 閱讀 `components/ask-assistant-chat/chats-messages.vue`：
        - 理解訊息列表如何依角色排版、解析文字／圖片／影片／音訊／附件內容，並顯示 reasoning、引用知識與 MCP 工具呼叫等資訊.
      - 閱讀 `components/ask-assistant-chat/chats-prompt/chats-prompt.vue`：
        - 確認輸入列如何處理文字輸入、Enter/Shift+Enter 行為、附件上傳（`usePromptFiles`）、文案優化（`apiOptimizeText`），以及 `needAuth` 導向登入邏輯.
      - 閱讀 `app/stores/chat.ts`：
        - 確認 `pendingConversation` 僅用於跨頁暫存「預先要發送的會話資訊」，並在 `[id].vue` onMounted 時自動帶入并送出.
      - 額外參考 `preview-chat.vue` 與 public agent `chat.vue` 中的 `useChat` 用法，以理解模式在其他情境的重用（不作為本 Feature C 主流程的一部分，只當參考）。
    - **Step 2：整理 chat 狀態管理與 API 呼叫點（已內化到文件結構中）**
      - 梳理 `messages` / `input` / `files` / `status` / `error` 的生命週期，以及 `selectedModel`、`selectedMcpIdList`、`pendingConversation` 在送出訊息與初始化時的行為.
      - 梳理 `apiGetAiConversation*`、`apiChatStream`、`apiGetChatConfig`、`apiGetQuickMenu` 在不同階段的用途.
    - **Step 3：回填 `docs/ui-flow-chat.md`**
      - 依照 A/B 文件風格，完成 chat 前端 UI flow 第一版（路由與畫面流／狀態與資料流／全域邏輯關聯／關鍵元件清單）。

### [驗證與結果]

- 目前仍未執行任何 `pnpm dev`、`pnpm build` 或測試指令，本 todo 聚焦在「閱讀前端程式與更新文件」，沒有 runtime 層面的驗證結果.
- Chat 前端 UI flow 的**主要畫面、狀態與資料流**已在 `docs/ui-flow-chat.md` 有第一版摘要，可支援後續：
  - 優化對話體驗（例如訊息排列、auto-scroll 行為、模型／MCP 選擇 UI）。
  - 規劃未來 C-API 深入 todo（針對 `/ai-chat/stream` 與 `ai-conversations` 相關後端實作做更細緻的 call stack 分析），或新增進階功能（多視窗、多 tab 比較等).
