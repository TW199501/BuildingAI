# TODO 2025-11-22 07

## 主題：Feature D：console - AI Provider 管理（前端 UI flow）

### [問題標題]

- 梳理後台 console 中 **AI Provider 管理頁面** 的 UI flow 與狀態／資料流，將結果整理到：
  - `docs/ui-flow-console-ai-provider.md`：描述 provider 列表、搜尋與篩選、批次操作、啟用／停用、建立／編輯供應商等流程。

### [背景與重現步驟]

- 目前狀態：
  - 已完成：
    - A：install（前端 + 後端初始化 API 深入）。
    - B：login/auth（前端 + 後端登入相關 API 深入）。
    - C：chat（前端 UI flow + 後端 chat C-API 深入）。
  - console 端尚未挑出具代表性的子系統來做 UI flow 深讀。
- 選定 D1 為：**AI Provider 管理** 子系統：
  - 理由：
    - 與 chat C-API 中使用的 `AiModel` / Provider / Secret 綁定高度相關，是 LLM 能否正常運作的基礎設施之一。
    - UI 涉及搜尋／篩選、批次刪除、啟用／停用、跳轉至 API Key 管理等互動，流程較完整。
- 目標：
  - 只看 **console 前端 UI 與 webapi 呼叫**，先不深入後端 Provider module 的實作。
  - 產出一份 `ui-flow-console-ai-provider.md`，結構對齊既有的 `ui-flow-install.md` / `ui-flow-login.md` / `ui-flow-chat.md`：
    - 流程目的與範圍。
    - 路由與畫面流。
    - 狀態與資料流。
    - 與全域邏輯（權限、overlay/modal、secret 管理）的關聯。
    - 重要畫面與元件清單。

### [相關檔案路徑與定位]

- 主要頁面與路由：
  - `packages/web/buildingai-ui/app/pages/console/ai/provider/list.vue`
    - Provider 列表主畫面：搜尋／篩選、卡片列表、批次選取、批次刪除、啟用／停用、打開編輯 modal、跳到 Model 列表等。
  - `packages/web/buildingai-ui/app/pages/console/ai/provider/edit.vue`
    - Provider 建立／編輯表單：
      - 基本資料（provider key、顯示名稱、icon、官網連結、描述等）。
      - API Key 綁定（Secret／KeyPool 選擇）。
      - 是否啟用、sort order。
      - 支援模型型別（supportedModelTypes）。
- 相關 console 元件與路由（僅作為上下文參考）：
  - `console/ai/provider/components/provider-card.vue`：Provider 卡片樣式與操作按鈕。
  - `console/ai/agent/**`：Agent 管理與 chat logs，雖然不是本 D 的主角，但有助理解 Provider 如何被上層功能使用。
  - `console/secret/**`：API Key / Secret 管理頁，`edit.vue` 會導向這裡（僅記錄連結，不在本 TODO 中展開 UI flow）。

### [分析與方案]

- 分析：
  - AI Provider 管理頁面是「設定可以使用哪些 LLM provider、與哪個 Secret 綁定、支援哪些 model types」的核心介面：
    - 與 **模型管理**、**chat C-API** 的實際運作密切相關。
    - 同時也是一個標準的「列表 + 搜尋過濾 + 卡片 + modal 表單」型頁面，適合當作 console UI flow 的代表案例。
  - 風險／注意事項：
    - 不要在第一次閱讀就深入所有 console 權限與 route config 細節，先聚焦在 provider 這條主線。
    - Secret / KeyPool 管理本身也很大，可以僅在本檔中標註「跳轉點」與整體關係，不展開其 UI flow。

- 方案：分兩階段完成

  1. **Phase 1：閱讀與記錄 provider 列表頁（list.vue）**

     - 目標：
       - 釐清列表頁的畫面結構：
         - 搜尋／篩選區（keyword + isActive）。
         - Provider 卡片網格 + loading / empty 狀態。
         - 底部統計與批次操作。
       - 確認狀態來源與 webapi 呼叫：
         - `apiGetAiProviderList`、`apiBatchDeleteAiProviders`、`apiDeleteAiProvider`、`apiToggleAiProviderActive`。
       - 了解 overlay/modal 的使用方式：
         - 點擊「新增」或「編輯」時如何開啟 `edit.vue` modal，回傳結果如何觸發列表刷新。
     - 作法：
       - 詳讀 `list.vue`：
         - 記錄畫面區塊：搜尋欄位、過濾 select、全選 checkbox、批次刪除按鈕、新增按鈕、Provider 卡片區、loading／empty／stats 區。
         - 梳理 `providers`、`selectedProviders`、`searchForm` 的 state 與 `getLists()` 的呼叫時機。
         - 釐清 AccessControl code 的角色（僅作權限遮罩，不深入）。

  2. **Phase 2：閱讀與記錄 provider 編輯頁（edit.vue）**

     - 目標：
       - 釐清 modal 內部表單結構與驗證邏輯：
         - 基本資料欄位。
         - API Key 綁定（KeyPoolSelect 與「前往 API Key 管理」按鈕）。
         - 啟用狀態切換、支援模型型別、多選下拉。
       - 梳理 `formData` 狀態、`providerSchema` 驗證，以及 `submitForm` 實際呼叫的 API：
         - `apiCreateAiProvider` / `apiUpdateAiProvider`。
     - 作法：
       - 詳讀 `edit.vue`：
         - 把 props / emits、`providerId` 計算方式、`fetchDetail` 讀取邏輯，以及 onMounted 行為整理清楚。
         - 記錄「新增 vs 編輯」在 UI 與 API 呼叫上的差異。

- 最終產出：
  - 依上述 Phase 1 + Phase 2 的閱讀結果，撰寫 `docs/ui-flow-console-ai-provider.md` 第一版，並在本 TODO 的「實作重點摘要／驗證與結果」補上閱讀成果。

### [實作重點摘要]

- 本 TODO 已完成 **「分析與方案」階段**：
  - 已實際通讀 `list.vue` / `edit.vue`。
  - 已補寫 `docs/ui-flow-console-ai-provider.md` 的實作細節。
- Provider 列表頁的畫面結構、狀態流（搜尋 / 選取 / 批次刪除）與 webapi 呼叫點。
- Provider 編輯 modal 的欄位、驗證規則與「新增／編輯」兩種模式的差異。
- console AI Provider flow 與 chat / C-API / Secret 管理之間的高階關係摘要。

### [驗證與結果]

- 目前仍未執行任何 `pnpm dev` / `pnpm build` 或實際進入 console 介面操作，本 TODO 聚焦在「閱讀程式與撰寫文件」，沒有 runtime 層面的驗證結果。
- 以靜態閱讀結果對照 `docs/ui-flow-console-ai-provider.md`，確認文件中描述的列表結構、搜尋／篩選／批次刪除／啟用切換與表單欄位，均能在 `list.vue` / `edit.vue` 中找到一一對應的實作。
- 與其他文件（`FRONTEND_ARCH.md`、`ui-flow-chat.md`、C-API 深入 TODO 等）的關聯目前僅為文字層級說明，未做實際操作驗證；若未來需要以實際 API 回應或畫面樣本補強，可再另開以驗證為主的 TODO.
