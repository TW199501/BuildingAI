# TODO 2025-11-22 02

## 主題：規劃前端核心流程 ui-flow 任務（A: install / B: login / C: chat / D: console 子系統）

### [問題標題]

- 規劃並分階段完成四個核心前端流程的 ui-flow 文件與程式閱讀：
  - A：install 安裝精靈流程
  - B：login / auth 登入驗證流程
  - C：chat 對話流程
  - D：console 某核心子系統流程

### [背景與重現步驟]

- 目前狀態：
  - `todo2025-11-22-01.md` 已完成專案初始理解與掃描計畫：
    - 建立並完成 `docs/PROJECT_OVERVIEW.md` 初版。
    - 建立並完成 `docs/FRONTEND_ARCH.md` 初版（含啟動流程、路由架構、狀態管理、pages 分區高階整理）。
  - 已初步掌握：
    - 前端主專案：`packages/web/buildingai-ui`。
    - Nuxt 啟動流程（`nuxt.config.ts`、`app/app.vue`）。
    - 全域路由 middleware（`app/middleware/route.global.ts`）。
    - Console 佈局與全域 Pinia stores 位置與職責概要。
    - `app/pages` 的主要分區：`index.vue`、`403.vue`、`[...all].vue`、`install/`、`login/`、`console/`、`chat/`、`profile/`、`public/`、`agreement/`、`micropage/`、`webview/`。
- 目標：
  - 不再只停留在「架構層級」，而是挑出 3–4 個實際業務流程，逐一深入閱讀對應的 pages / components / stores / API 呼叫，並將流程圖與資料流整理到 `docs/ui-flow-<feature>.md` 中。
  - 先在本檔規劃四個 feature 的處理順序與對應 ui-flow 檔名，暫不閱讀新程式碼，維持「分析與方案」階段。

### [相關檔案路徑與定位]

- 既有 overview / arch 文件：
  - `docs/PROJECT_OVERVIEW.md`
  - `docs/FRONTEND_ARCH.md`
- 相關前端程式碼目錄（未在本 todo 中展開細節，僅作為後續參考）：
  - `packages/web/buildingai-ui/app/pages/install/**`
  - `packages/web/buildingai-ui/app/pages/login/**`
  - `packages/web/buildingai-ui/app/pages/chat/**`
  - `packages/web/buildingai-ui/app/pages/console/**`
  - 以及與上述頁面互動的 stores / utils / services（視未來實際需要展開）。

### [分析與方案]

- 分析：
  - 根據 `ELF_AI_ONBOARDING.md`，`ui-flow-<feature>.md` 適合用在「少數、需要深入理解的核心功能」，而不是為所有頁面一一建立。
  - 目前選定的四個 feature：
    - install：影響系統初始化與整體可用性，是整個系統的入口。
    - login/auth：幾乎所有受保護頁面都會經過，牽涉 user store、token、redirect 等邏輯。
    - chat：最貼近使用者體驗的 AI 對話流程，是對外展示的重要部分。
    - console 子系統：實際營運與配置的後台核心之一，但具體要選哪個子模組可在之後明確指定。
  - 為避免一次展開過多任務，本 todo 僅負責：
    - 確認四個 feature 的大致範圍與處理順序。
    - 定義對應的 ui-flow 檔名與預期內容。
    - 將實際程式閱讀與 ui-flow 寫作放到後續 todo 步驟逐一完成。

- 方案：依照 A → B → C → D 的順序逐一處理

  1. **Feature A：install 安裝精靈流程**

     - 範圍預期：
       - `app/pages/install/**` 相關頁面。
       - 與系統初始化檢查邏輯（`appStore.checkSystemInitialization()`、`route.global.ts`）的互動。
       - 可能涉及後端 API 中的「初始化／安裝」相關 endpoint（僅在需要時閱讀）。
     - 規劃中的 ui-flow 檔名：
       - `docs/ui-flow-install.md`
     - 預期內容（未來填寫）：
       - 路由與畫面流：使用者如何從 `/install` 進入並完成安裝。
       - 狀態與資料流：安裝過程中需要填寫／送出的設定，以及如何影響系統狀態。
       - 相關 API 呼叫與錯誤處理重點。

  2. **Feature B：login / auth 登入流程**

     - 範圍預期：
       - `app/pages/login/**` 相關頁面。
       - `user` store（`packages/web/@buildingai/stores/src/user.ts`）的登入／登出／token 刷新邏輯。
       - `route.global.ts` 中與登入、權限、redirect 相關的流程。
     - 規劃中的 ui-flow 檔名：
       - `docs/ui-flow-login.md`
     - 預期內容（未來填寫）：
       - 使用者從未登入狀態到登入完成的整條路徑（包含 redirect query）。
       - token / cookie / loginTimeStamp 的更新與過期處理方式。
       - 無權限 / session 過期時的導向行為（/login、/403 等）。

  3. **Feature C：chat 對話流程**

     - 範圍預期：
       - `app/pages/chat/**` 相關頁面與組件。
       - 相關 stores（若存在，例如目前尚未細讀的 chat 專用 store）。
       - 與後端 chat / 對話 API 的互動（收發訊息、流式回應等）。
     - 規劃中的 ui-flow 檔名：
       - `docs/ui-flow-chat.md`
     - 預期內容（未來填寫）：
       - 使用者從選擇模型／場景到發送訊息、接收回應的流程圖。
       - 對話歷史、訊息列表、狀態（loading / streaming / error）的處理方式。
       - 重要 UI 互動（例如重新生成、停止、插入工具結果等）。

  4. **Feature D：console 某核心子系統流程**

     - 範圍預期：
       - `app/pages/console/**` 底下的某一個子系統（例如：模型管理、知識庫管理、Agent 管理、MCP 工具管理等）。
       - 與 `permissionStore`、menu 組態以及動態路由註冊的互動（`menu-helper.ts`、`route.global.ts`）。
     - 規劃中的 ui-flow 檔名（暫定，實際名稱在選定子系統時再具體化）：
       - `docs/ui-flow-console-<feature>.md`
     - 預期內容（未來填寫）：
       - 對應子系統的導航結構（從 console menu 到實際頁面）。
       - 各主要操作（新增／編輯／刪除／搜尋／授權等）的 UI 流程與資料流。
       - 與後端 API 的對應關係與權限控制重點。

- 總結：
  - 本 todo 不直接進行任何一個 feature 的程式細讀或實作，只負責：
    - 將 A/B/C/D 四個 feature 的任務列出與排序。
    - 定義後續會使用的 `ui-flow-*.md` 檔案名稱與預期內容框架。
  - 實際展開時，每個 feature 都會：
    - 在對應的 todo 項目中（參見 todo list）更新「實作重點摘要／驗證與結果」.
    - 在對應的 `ui-flow-*.md` 中記錄具體程式細節與流程圖.

### [實作重點摘要]

- 本檔原始目標為「分析與方案」，目前已開始執行 Feature A（install flow）的實際程式閱讀與 ui-flow 撰寫，仍未修改任何程式碼.
- 已完成的工作：
  - 明確列出四個優先處理的前端流程 feature（A–D），並排序為 A → B → C → D.
  - 為每個 feature 規劃對應的 `ui-flow-*.md` 名稱與預期內容方向：
    - A：`docs/ui-flow-install.md`
    - B：`docs/ui-flow-login.md`
    - C：`docs/ui-flow-chat.md`
    - D：`docs/ui-flow-console-<feature>.md`（待選定具體子系統時再具體命名）.
  - Feature A（install flow）相關：
    - 閱讀 `app/pages/install/index.vue`、`use-install.ts` 與 `components/step-0.vue`、`step-1.vue`、`step-2.vue`、`step-3.vue`，整理出四個步驟的實際畫面流與互動邏輯.
    - 在 `docs/ui-flow-install.md` 中補齊：
      - 路由與畫面流：從 `/install` 入口、Step0 歡迎頁、Step1 管理員帳號設定、Step2 網站資訊設定到 Step3 安裝完成頁的完整流程，以及成功後導向 `/` 或 `/console` 的行為.
      - 狀態與資料流：`useInstall()` 中的 `adminForm`、`websiteForm` 與 `isSubmitting*` 狀態，及 `apiInitializeSystem` 的請求資料與回應處理（成功時設定 token、刷新 config 與 systemInfo，並回報安裝統計）.
      - 與全域邏輯的關聯：`checkSystemInitialization()` 如何在未初始化時導向 `/install`，以及安裝成功後如何避免再次進入安裝流程.
      - 重要畫面與組件清單：Step0–Step3 以及 TrueFocus、BdUploader、BdInputPassword 等輔助元件的角色.
  - Feature B（login/auth flow）相關：
    - 閱讀 `app/pages/login/index.vue`、`components/account/account.vue`、`components/wechat/wechat.vue`、`components/login-bind.vue`，以及 user store（`@buildingai/stores/src/user.ts`）與 `route.global.ts` 中 `handleAuth()` 的登入導向邏輯.
    - 在 `docs/ui-flow-login.md` 中補齊：
      - 路由與畫面流：從未登入情境被導向 `/login?redirect=...`，到登入成功後依 `redirect` 參數或預設 HOME 路徑導向目標頁面的完整流程，包含帳號登入、微信掃碼登入與強制綁定手機（login-bind）三種子流程.
      - 狀態與資料流：user store 中 `token`、`temporaryToken`、`userInfo`、`loginTimeStamp`、`isAgreed` 等狀態，與 `login`、`tempLogin`、`logout`、`refreshToken` 等方法在登入／登出過程中的實際作用.
      - 與全域路由邏輯的關聯：`handleAuth()` 如何在未登入時導向 `/login`，在已登入時避免再次停留在登入頁，並於進入其他頁面時適時呼叫 `refreshToken()` 延長 token.
      - 重要畫面與組件清單：login index 頁、AccountLogin 容器元件、WeChat 登入元件與 LoginBind 綁定手機元件的職責分工.

### [驗證與結果]

- 目前仍未執行任何 `pnpm dev`、`pnpm build` 或測試指令，本 todo 聚焦在「規劃＋閱讀程式並撰寫文件」，無實際執行結果可驗證.
- Feature A（install flow）階段性結果：
  - 尚未深入對照後端 `apiInitializeSystem` 的欄位細節與資料庫層實作，若未來有需要針對安裝流程做變更，可再開新 todo 專門追後端部分並在本檔或 ui-flow 檔補充.
- Feature B（login/auth flow）階段性結果：
  - 已在 `docs/ui-flow-login.md` 中形成第一版登入流程說明，涵蓋帳號登入、微信掃碼登入與強制綁定手機的前端行為與 user store 狀態流，但尚未深入對照實際登入 API 與錯誤碼設計.
- 後續建議（依原先規劃，留待未來新 todo 視需求展開）：
  - 完成 Feature A 如需更深入（例如對應資料庫 schema 或錯誤碼）：
    - 新開以 install 為主題的 todo，閱讀後端初始化模組並更新 `ui-flow-install.md` 或相關 API 文件.
  - 完成 Feature B 如需更深入（例如登入 API 的安全策略、風控與錯誤處理）：
    - 新開以 login/auth 為主題的 todo，閱讀後端登入模組與相關中介層，再在 `ui-flow-login.md` 或 API 文件中補齊後端視角.
  - 之後依序展開 Feature C / D，沿用同樣模式：
    - 先閱讀前端頁面與 stores.
    - 再閱讀必要的後端 API.
    - 最後在各自的 `ui-flow-*.md` 中補齊流程與資料流.
